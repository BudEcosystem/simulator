streamlit
matplotlib>=3.7.0
plotly>=5.17.0
seaborn>=0.12.0
plotnine
tqdm>=4.66.0
paretoset
kaleido
nbformat
numpy>=1.24.0
pandas>=2.0.0
pillow>=10.1.0
ipywidgets
Jinja2
tabulate
fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.5.0
requests>=2.31.0
beautifulsoup4>=4.12.2
json_repair
lxml
python-multipart>=0.0.6
aiofiles>=23.2.1
httpx>=0.25.2
colorama>=0.4.6
psutil>=5.9.0

pytest>=7.4.3
pytest-asyncio>=0.21.1
python-dotenv>=1.0.0
watchdog>=3.0.0
sqlalchemy>=2.0.0
alembic>=1.12.0
transformers>=4.35.0
torch>=2.0.0
accelerate>=0.24.0
huggingface-hub>=0.19.0

# --- Core deps the code imports that were previously missing (a fresh install would crash) ---
scipy>=1.10.0          # validation/calibration: scipy.optimize / scipy.stats (module-level imports)
PyYAML>=6.0            # Scale-Sim / ASTRA-Sim + training config readers (module-level `import yaml`)
packaging>=23.0        # version parsing across the engine
pymoo>=0.6.0           # NSGA-II hardware/config design-space search (else silent coarse-grid fallback)
slowapi>=0.1.9         # API rate limiting (apis/main.py; degrades gracefully if absent)
openai>=1.0.0          # optional LLM provider for model auto-config (src/bud_ai.py); used when LLM_PROVIDER=openai

# --- Optional extras (not required to run; install on demand) ---
# openevolve            # LLM-driven algorithm evolution in BudEvolve (heavy; code falls back gracefully)
# bud_models            # external Bud model-registry integration (private package)
